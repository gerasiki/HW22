Я изучила работу https://github.com/blazkowicz-pnz/hw21
И обнаружила следующие запахи:

- Файл main.py 94 - 148
- Длинный метод
- я бы:
Разделила main() на логические части и вынесла их в отдельные вспомогательные функции,
либо классы. В частности: строки с 107 по 127 можно вынести в отдельный класс, который
будет заниматься проверкой наличия товара и места на складе и в магазине.
Строки с 130 по 133 можно вынести в функцию, которая осуществляет перемещение товара.
У меня она названа - logistic().
Строки с 137 по 146 тоже выносим в отдельную функцию, которая выводит статистику - print_statistic().

- Файл main.py 137 - 146
- Дублирование кода
- я бы:
Если вынести эту часть в отдельную функцию - можно вызвать эту функцию два раза, с соответствующими
аргументами: print_statistic(from_), print_statistic(to).

- Других запахов я не нашла. Однако, я бы:
Разбила один длинный код на логические элементы и разместила бы их в разных файлах. Например так:
main.py - содержит цикл и запуск цикла
classes.py - все основные классы (Storage, Store, Shop, Request)
help_class.py - CheckProduct класс для проверки товара, который выбрал пользователь.
utils.py - функции с принтом логистики и статистики.

- Кроме этого лучше убрать break с 148 строки и перенести store.items = store_items из main() на 160.
- Также, если бы у нас был большой магазин, store_items переехали бы в отдельный файл.

К ревью прилагаю переделанный вариант в another_solution